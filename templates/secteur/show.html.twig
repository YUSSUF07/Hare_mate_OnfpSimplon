{% extends 'base.html.twig' %}

{% block title %}Secteur{% endblock %}

{% block body %}
    <h2>Secteur</h2>
    
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                
            </div>
            <div class="col-md-4">
                <table class="table">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ secteur.id }}</td>
                        </tr>
                    </tbody>
                </table>
                <a href="{{ path('Secteur_edit', {'id': secteur.id}) }}" class="btn btn-primary">Editer la Secteur</a>
            </div>
        </div>
        
    </div>
    <a href="{{ path('secteur_index') }}">Retouner</a>

    

    {{ include('secteur/_delete_form.html.twig') }}
{% endblock %}
